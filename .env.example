# Server
PORT=5000
NODE_ENV=development
JWT_SECRET=change_this_to_a_long_random_secret

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=placement_readiness_db

# Python skill extractor (invoked as a child process)
PYTHON_BIN=python3
